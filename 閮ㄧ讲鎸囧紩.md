# 体系工程师工作台 - 部署指引

## 一、GitHub Pages 部署（推荐）

### 步骤 1：创建 GitHub 仓库
1. 登录 [GitHub](https://github.com)，点击 **New repository**
2. 仓库名填 `qms-workbench`（可自定义）
3. 选择 **Public**（免费Pages）或 **Private**（需Pro）
4. 勾选 **Add a README file**
5. 点击 **Create repository**

### 步骤 2：上传工作台文件
将 `workbench` 文件夹下的全部文件上传到仓库：
- `index.html`（主文件）
- `manifest.json`（PWA配置）
- `sw.js`（离线缓存）
- `icon.svg`（应用图标）

**上传方法**（任选其一）：
- 网页拖拽：仓库页面 → Add file → Upload files → 拖入文件
- Git命令：
  ```bash
  git clone https://github.com/你的用户名/qms-workbench.git
  # 把4个文件复制进去
  git add .
  git commit -m "添加工作台"
  git push
  ```

### 步骤 3：开启 GitHub Pages
1. 仓库 → **Settings** → 左侧 **Pages**
2. Source 选 **Deploy from a branch**
3. Branch 选 `main`，文件夹选 `/ (root)`
4. 点击 **Save**
5. 等待1-2分钟，页面顶部会显示访问地址：
   ```
   https://你的用户名.github.io/qms-workbench/
   ```

### 步骤 4：手机/iPad 添加桌面图标
1. 用手机/iPad 打开上面的网址
2. **iPhone/iPad（Safari）**：点分享按钮 → 添加到主屏幕
3. **Android（Chrome）**：点菜单 → 添加到主屏幕
4. 桌面出现"QMS工作台"图标，点击直接全屏打开

---

## 二、数据同步配置（多设备同步）

### 为什么需要同步？
工作台数据存在浏览器 LocalStorage，**不同设备数据独立**。配置同步后，数据存到 GitHub 仓库的 `data.json` 文件，各设备可上传/拉取。

### 步骤 1：创建 Personal Access Token
1. GitHub → 右上角头像 → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. 点 **Generate new token (classic)**
3. Note 填 `qms-sync`
4. Expiration 选 `No expiration`（或按需）
5. 勾选 **repo**（完整仓库权限）
6. 点 **Generate token**
7. **立即复制** token（形如 `ghp_xxxxxxxx`），关闭后无法再看到

### 步骤 2：在工作台配置同步
1. 打开工作台，点击顶部 **☁️ 同步** 按钮
2. 首次会弹出配置框，填入：
   - GitHub 用户名
   - 仓库名（如 `qms-workbench`）
   - 分支名（默认 `main`）
   - Token（粘贴刚才复制的）
3. 点 **保存配置**

### 步骤 3：同步操作
- **上传（电脑→云端）**：点 ☁️ 同步 → 上传成功
- **拉取（云端→手机）**：手机打开工作台 → 点 ☁️ 同步 → 选"是"拉取
- 同步状态显示在按钮旁边

### 数据同步说明
| 场景 | 操作 |
|---|---|
| 电脑录入完 → 手机查看 | 电脑点同步（上传）→ 手机点同步（拉取） |
| 手机临时记录 → 电脑继续 | 手机点同步（上传）→ 电脑点同步（拉取） |
| 换新设备 | 新设备打开网址 → 配置同步 → 拉取 |

⚠️ **注意**：同步是**手动覆盖式**，不是实时自动同步。每次换设备工作前先拉取，工作完上传。

---

## 三、文件清单

| 文件 | 作用 | 必须 |
|---|---|---|
| `index.html` | 工作台主页面（全部功能） | ✅ |
| `manifest.json` | PWA配置（主屏图标、全屏） | ✅ |
| `sw.js` | Service Worker（离线缓存） | ✅ |
| `icon.svg` | 应用图标 | ✅ |
| `部署指引.md` | 本文档 | ❌ |

---

## 四、常见问题

**Q: 手机打开是白屏？**
A: 检查文件是否全部上传，特别是 `manifest.json` 和 `sw.js`。

**Q: 同步失败提示401？**
A: Token无效或过期，重新生成Token并更新配置。

**Q: 同步失败提示404？**
A: 仓库不存在或文件路径错误，检查用户名/仓库名拼写。

**Q: 数据会丢失吗？**
A: 不会。本地数据存 LocalStorage（刷新不丢），同步后还有云端备份。建议定期点同步备份。

**Q: 可以不用GitHub吗？**
A: 可以只用本地文件（双击index.html打开），但手机无法访问，数据不能同步。
